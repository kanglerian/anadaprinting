import { Center } from "@chakra-ui/react";
import NotFound from "./NotFound";

const Unknown = () => {
    return (
        <Center w='350px' h='410px'>
            <NotFound />
        </Center>
    );
}

export default Unknown;